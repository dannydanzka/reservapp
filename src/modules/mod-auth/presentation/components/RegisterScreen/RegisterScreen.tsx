import React from 'react';

import { Container, Subtitle, Title } from './RegisterScreen.styled';

export const RegisterScreen: React.FC = () => {
  return (
    <Container>
      <Title>Register Screen</Title>
      <Subtitle>Esta será la pantalla de registro cuando implementemos la UI</Subtitle>
    </Container>
  );
};
